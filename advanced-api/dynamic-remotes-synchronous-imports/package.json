{
  "name": "dynamic-remotes-synchronous-imports",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "lerna run --scope=dynamic-remotes-synchronous-imports_* --parallel start",
    "build": "lerna run --scope=dynamic-remotes-synchronous-imports_* --parallel build",
    "serve": "lerna run --scope=dynamic-remotes-synchronous-imports_* --parallel serve",
    "clean": "lerna run --scope=dynamic-remotes-synchronous-imports_* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
